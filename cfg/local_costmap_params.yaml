local_costmap:
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 5.0
  publish_frequency: 2.0
  rolling_window: true # follows robot while navigating
  width: 6.0
  height: 6.0
  resolution: 0.05
  meter_scoring: true
  transform_tolerance: 0.5 #maximum amount of latency allowed between tf